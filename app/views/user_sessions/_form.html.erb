<%= simple_form_for @user do |f|%>
    <%= f.input :email, placeholder: 'e.g. bigbro@broski.com' %>
    <%= f.input :password, hint: '* must be at least six characters long' %>
    <%= f.submit "Login" %>
<% end %>